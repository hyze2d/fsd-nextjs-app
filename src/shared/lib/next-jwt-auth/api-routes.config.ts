const loginPath = '/api/auth/login';
const refreshPath = '/api/auth/refresh';
const logoutPath = '/api/auth/logout';

const apiRoutes = {
  loginPath,
  refreshPath,
  logoutPath
};

export { apiRoutes };
