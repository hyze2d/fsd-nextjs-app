import type { ThemeOptions } from '@shared/ui/theme';

const light: ThemeOptions = {
  primary: '#000',
  secondary: '#000',
  teritary: '#000',
  quaternary: '#000',
  text: '#000',
  info: '#000',
  success: '#000',
  warning: '#000',
  error: '#000'
};

const dark: ThemeOptions = {
  primary: '#000',
  secondary: '#000',
  teritary: '#000',
  quaternary: '#000',
  text: '#000',
  info: '#000',
  success: '#000',
  warning: '#000',
  error: '#000'
};

const theme = {
  dark,
  light
};

export { theme };
